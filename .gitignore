__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
*.npz
*.safetensors
data/*
# Probe question sets are curated source, not data artifacts.
!data/probes/
# Hand-written calibration corpora are source too (downloaded sets are not).
!data/corpora/
datasets/
pruned_*/
.claude/
skills/
local/
bench/
uv.lock

# User-specific scripts (not committed)
scripts/

